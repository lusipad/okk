## 1. CLI MVP 主路径

- [x] 1.1 实现 `partner home / team / summary` 等赛博同事视图命令
- [x] 1.2 实现 `mission list / new / show / continue` 等任务主路径命令

## 2. 协作执行与监督

- [x] 2.1 实现 `run direct / team`，并补齐默认多同事编排模板
- [x] 2.2 实现 `checkpoint list` 与任务整体进度输出

## 3. 启动与 dog-fooding

- [x] 3.1 补齐 Windows 的 `cmd + ps1` 启动器
- [x] 3.2 用 OKK CLI 自己创建 mission、发起 team run、查看 checkpoint，验证自监督闭环
